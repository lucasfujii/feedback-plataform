import { createMuiTheme } from '@material-ui/core/styles';

const color = {
    primary: "#4040ff !important",
}

const theme = createMuiTheme({
    color: {
        primary: color.primary,
    },
    
});

export default theme;